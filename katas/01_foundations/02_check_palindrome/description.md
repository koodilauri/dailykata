Write a function `isPalindrome` that returns `true` if the string reads the same forwards and backwards, and `false` otherwise. Ignore case.

Examples:
  isPalindrome("racecar")  // true
  isPalindrome("hello")    // false
  isPalindrome("Madam")    // true
