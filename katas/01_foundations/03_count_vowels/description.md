Write a function `countVowels` that counts the number of vowels (a, e, i, o, u — case-insensitive) in a string.

Examples:
  countVowels("hello")   // 2
  countVowels("rhythm")  // 0
  countVowels("AEIOU")   // 5
