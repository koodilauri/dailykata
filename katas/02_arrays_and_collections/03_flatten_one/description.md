Write a function `flattenOne` that flattens an array one level deep — nested arrays are unwrapped, but only one level.

Examples:
  flattenOne([1, [2, 3], [4]])      // [1, 2, 3, 4]
  flattenOne([[1, [2]], [3]])        // [1, [2], 3]
  flattenOne([1, 2, 3])             // [1, 2, 3]
