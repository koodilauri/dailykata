Write a function `binarySearch` that searches for a `target` in a sorted array of numbers. Return the index of the target, or `-1` if not found.

Examples:
  binarySearch([1,3,5,7,9], 5)   // 2
  binarySearch([1,3,5,7,9], 6)   // -1
  binarySearch([2], 2)            // 0
