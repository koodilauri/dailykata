Implement a `Queue` class with the following methods:
- `enqueue(value)` — add a value to the back
- `dequeue()` — remove and return the front value (undefined if empty)
- `front()` — return the front value without removing it (undefined if empty)
- `isEmpty()` — return true if the queue has no elements
- `size()` — return the number of elements
