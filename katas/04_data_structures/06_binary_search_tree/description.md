Implement a `BST` (Binary Search Tree) class:
- `insert(value)` — add a number maintaining BST order
- `search(value)` — return true if the value exists
- `inorder()` — return all values via in-order traversal (sorted ascending)

Use `BSTNode` with `value`, `left`, and `right` properties.
